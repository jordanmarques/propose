import React from "react";

const CenteredPageLarge = ({children}) => {
    return (
        <div className="container-fluid">
            {children}
        </div>
    )
};

export default CenteredPageLarge
