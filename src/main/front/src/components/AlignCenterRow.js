import React from 'react';

const AlignCenterRow = ({children}) => {
    return (
        <div className="align-center-row">
            {children}
        </div>
    );
};

export default AlignCenterRow;
