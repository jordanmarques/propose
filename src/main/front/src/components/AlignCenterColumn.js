import React from 'react';

const AlignCenterColumn = ({children}) => {
    return (
        <div className="align-center-column">
            {children}
        </div>
    );
};

export default AlignCenterColumn;
