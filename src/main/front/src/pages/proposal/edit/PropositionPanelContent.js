import posed from 'react-pose'

const PropositionPanelContent = posed.div({
    closed: { height: 0, marginTop: 0},
    open: { height: "auto", marginTop: 20}
});

export default PropositionPanelContent;
